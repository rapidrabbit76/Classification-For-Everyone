method: grid
metric:
  goal: maximize
  name: test_acc
parameters:
  model_type:
    values: [s, m, l, lx]
  gradient_clip_val:
    values:
      - 2
      - 4
  precision:
    values:
      - 16
      - 32

program: train.py
